module Nrtflrx
  class Catalog
    def request
      ''
      # Nrtflrx::Request.new(resource_path, http_verb).execute
      # needs: http verb, base url, resource path, oauth consumer key,
      # oauth nonce, oauth signature method, oauth timestamp, oauth version,
      # and oauth signature
    end
  end
end
